{
  "_comment": "範例 Cursor MCP 配置檔案 - 實際端口會是動態分配的",
  "_location": "將此內容複製到 ~/.cursor/mcp.json",
  "_note": "擴展會自動建立和更新此配置，通常無需手動設定",
  
  "servers": {
    "vscode-commands": {
      "url": "http://127.0.0.1:3000/mcp/sse",
      "transport": "sse",
      "_description": "實際 URL 會根據動態分配的端口自動更新"
    }
  }
}
